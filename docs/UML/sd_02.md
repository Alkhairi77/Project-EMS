sequenceDiagram
    autonumber
    actor Org as Organizer
    participant View as Create Event Form (Blade)
    participant Ctrl as EventController
    participant Store as Local Storage (Public Disk)
    participant DB as MongoDB (events)

    Org->>View: Isi Form Acara & Unggah Gambar Banner
    Org->>View: Klik Tombol "Save Event"
    activate View
    View->>Ctrl: HTTP POST /organizer/events (Multipart Form)
    deactivate View
    activate Ctrl
    Ctrl->>Ctrl: Validasi Input (Tanggal depan, tipe data, dll)
    
    Ctrl->>Store: Simpan File Gambar ke /public/banners
    activate Store
    Store-->>Ctrl: Kembalikan Relative Path URL String
    deactivate Store

    Ctrl->>DB: Insert Dokumen Baru (BSON dengan user_id & banner path)
    activate DB
    DB-->>Ctrl: Sukses Simpan Data
    deactivate DB

    Ctrl-->>Org: Redirect ke Index Event dengan Flash Message Sukses
    deactivate Ctrl
